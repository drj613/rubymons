Practicing connecting to external APIs in Ruby via HTTParty using https://pokeapi.co

Currently only gets BSTs by running `ruby endpoints/pokemon.rb` but it'll hopefully have a full-fledged CLI eventually.
